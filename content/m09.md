::: objectives
- Identify context isolation, not specialisation, as the real benefit of multi-agent systems
- Predict when orchestrator-workers wins and when it burns money for nothing
- Write delegation prompts precise enough that workers do not duplicate each other
- Embed effort-scaling heuristics so worker count matches task size
- Synthesise conflicting findings without losing citations
- Find the knee on the quality-cost curve and defend a worker count with numbers
:::

## The real benefit is not specialisation

The usual pitch for multi-agent systems is specialisation: a research agent, a writing agent, a
critic. That framing is mostly wrong, because a single model with a good prompt can play all three
roles competently.

The actual benefit is **context isolation**. Each sub-agent gets its own window. A worker can read
twenty documents, burn 80,000 tokens exploring, and return a two-paragraph finding. The
orchestrator's context grows by two paragraphs.

That is the trick. Not "the researcher is better at researching" but "the researcher's mess stays in
the researcher's window."

::: key
Multi-agent systems can burn many times the tokens of a single chat. The task has to be worth it.
:::

## When it works

On wide search tasks — where the work is finding and reading many sources in parallel — token budget
explains most of the performance variance between systems. If the task's difficulty is proportional
to how much material must be examined, then more parallel context is directly more capability.

Under those conditions, an orchestrator running a strong lead model with a pool of cheaper workers
has been measured substantially ahead of a strong single agent on research benchmarks. The mechanism
is unglamorous: the multi-agent system simply read more, and the lead saw only the conclusions.

The conditions that make it work, all of which must hold:

- The task **parallelises**. Independent sub-questions, not a chain where step 2 needs step 1.
- The work is **read-heavy**. Gathering and filtering, not a long sequence of mutations.
- The task is **valuable enough** to absorb the token multiple.

## When it fails

**Duplicated work.** Two workers given overlapping briefs run nearly the same searches and return
nearly the same findings. You paid twice for one result. This is the most common failure and it is a
prompt problem, not a code problem.

**Contradictory findings.** Worker A says the figure is 2.1B, worker B says 2.15B. Now the
orchestrator must adjudicate — and its most common instinct is to average them or pick the
better-written one, both of which are wrong.

**Coordination overhead.** Delegating, waiting, synthesising all cost turns and tokens. Below a
certain task size, the overhead exceeds the benefit and one agent doing it directly is faster and
better.

**Synchronous bottlenecks.** An orchestrator that waits for all workers runs at the speed of the
slowest, and one hung worker stalls everything.

**Cost.** The multiple is real. Budget for it before building, not after the first invoice.

## The orchestrator-worker shape

```python title=orchestrator.py
def run(lead, worker_pool, task):
    plan = lead.plan(task)                       # decides subtasks AND how many
    briefs = plan["subtasks"]

    findings = run_concurrently(
        worker_pool.run(brief) for brief in briefs
    )                                             # failures return a marker, never raise

    return lead.synthesise(task, findings)


def effort(task_kind):
    """Worker count is a decision, not a constant."""
    return {
        "simple_fact":      1,     # one lookup; a pool here is pure waste
        "comparison":       3,     # one per thing compared
        "open_research":    8,     # genuinely wide; cap it
    }[task_kind]
```

Two details in that sketch carry most of the weight. Worker failures return a marker rather than
raising, so one dead worker degrades the answer instead of killing the run. And worker count is
computed from the task, not fixed — which is the next section.

## Delegation prompts are the hard part

This is where multi-agent systems are actually built or lost. A vague brief produces overlapping,
unciteable, unusable findings, and no amount of orchestrator cleverness recovers from it.

A worker brief needs five things:

```text
TASK: Find the FY2025 revenue figure for Acme Corp and its year-over-year change.

SCOPE: Revenue only. Do NOT investigate margins, headcount, competitors or
       guidance — other workers are covering those. If you encounter relevant
       material outside your scope, note the source and move on.

TOOLS: web_search, fetch_document. You may not write files or send messages.

EFFORT: This is a single-fact lookup. Expect 2-4 searches. Stop when you have a
        figure from a primary source; do not keep searching for corroboration
        beyond two independent sources.

OUTPUT: JSON — {"figure": str, "yoy_change": str, "sources": [{"url": str,
        "quote": str, "date": str}], "confidence": "high"|"medium"|"low",
        "caveats": str}
```

**Scope with explicit negative space.** "Revenue only, others are covering margins" is what prevents
duplication. Without it, every worker helpfully investigates the interesting adjacent thing.

**Tool restrictions per worker.** Read-only workers should be read-only in configuration, not by
request. This is also a security control (M11): a worker exposed to untrusted web content should not
be holding write tools.

**Effort scaling, stated.** Without a stated expectation, workers run until they feel finished — and
"finished" for a language model on an open-ended search is a long way out. Say roughly how much work
this is worth.

**A structured output contract.** The orchestrator must merge findings. Prose does not merge.

**Confidence and caveats as required fields.** They give the lead something to adjudicate with when
findings conflict, instead of guessing.

## Effort-scaling heuristics

Fixed worker counts are how you get eight agents on a question one lookup would answer, and one agent
on a question needing twenty. Put the scaling rule in the orchestrator's prompt:

```text
Choose the number of subagents by the shape of the task:

  a single fact, one source           -> 1 subagent
  a comparison across N things        -> N subagents, one each, N <= 6
  open-ended research                 -> 5-10, split by sub-question
  a survey with a known list          -> one per item, batched, cap 12

Do not exceed 12. If the task seems to need more, the decomposition is wrong —
split it into two rounds instead.
```

::: widget worker-knee
:::

## Synthesis is the lead's hardest job

Everything so far was preparation for this. The lead now holds N findings and must produce one
answer.

**Conflict resolution needs a rule, stated in advance.** Prefer primary sources over secondary. Prefer
recent over old *for facts that change*. Prefer the higher-confidence finding when sources are
equally good. When a conflict cannot be resolved, **surface it** — "sources disagree: the 10-K says
2.1B, the press release says 2.15B" is a far better answer than a confident average.

**Citations must survive.** The lead is rewriting text, and rewriting is exactly when attributions
get dropped. Require the synthesis to carry source references through to the final output; assert it
in the eval suite.

**And the failure to design against explicitly:** three workers return confidently wrong findings and
the lead synthesises them into a fluent, well-structured, entirely false answer. The multi-agent
structure *amplified* the error, because agreement between workers reads as corroboration. Defences:
require sources for every claim, have the lead check whether workers actually used independent
sources, and treat unanimous confidence on a hard question as suspicious rather than reassuring.

## The decision rule

Before you build one, three things must be true:

1. **Parallelisable** — genuine independent subtasks.
2. **Read-heavy** — gathering and filtering, not a mutation sequence.
3. **Worth the multiple** — the value per task can absorb several times the single-agent cost.

If all three hold, build it. If not, a single agent with good context management (M3) will be
cheaper, faster and easier to debug.

::: autopsy Two subagents, same search, twice the bill
Two workers, briefs "research the competitive landscape" and "analyse market positioning". A human
reads those as different questions. The workers ran seventeen searches between them, thirteen of
them identical, and returned findings that overlapped by roughly 80%.

The fix was not in code. It was a scope line in each brief naming what the *other* worker was
covering, and a shared "already searched" list passed to each. Duplicate searches dropped to two, at
no cost in quality. The lesson generalises: nearly every multi-agent efficiency bug is a delegation
prompt bug, and the reflex to fix it with orchestration code is a reflex to fix it in the wrong
place.
:::

::: lab Lab 9 — Build a research system
`labs/lab09_multiagent` — an orchestrator plus a configurable worker pool. Required: explicit effort
heuristics, per-subagent tool restrictions enforced in configuration, citation preservation through
synthesis, and graceful degradation when a worker fails or hangs.

The gate has two parts and both must pass: beat the single-agent baseline on the rubric, **and** come
in under the cost-per-task ceiling. Quality alone is not a pass.
:::

::: lab Lab 9b — The economics report
Run the same 15 tasks at 1, 3 and 8 workers. Plot quality against cost, find the knee, and defend a
recommendation. `python -m lab09_multiagent.economics` produces the table and the chart data.

The knee is always lower than students expect. Notice your own prior before you run it, then notice
how wrong it was.
:::

## Further reading

- [How we built our multi-agent research system](https://www.anthropic.com/engineering/multi-agent-research-system)
- [Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)

::: quiz
[
  {
    "q": "What is the primary benefit of an orchestrator-worker architecture?",
    "options": [
      "Specialised agents are individually better at their subtasks",
      "Context isolation — a worker's exploration stays in the worker's window, and only the finding reaches the lead",
      "It is cheaper than a single agent",
      "It removes the need for a turn cap"
    ],
    "answer": 1,
    "why": "A single model with a good prompt plays all the specialist roles competently, so specialisation is not the win. The win is that a worker can burn 80,000 tokens reading and return two paragraphs — the orchestrator's context grows by two paragraphs. It is markedly more expensive, not less."
  },
  {
    "q": "Two workers are briefed \"research the competitive landscape\" and \"analyse market positioning\". What happens, and where is the fix?",
    "options": [
      "They divide the work sensibly; no fix needed",
      "They largely duplicate each other's searches; the fix is scope with explicit negative space in each brief",
      "They contradict each other; the fix is a tie-break rule in synthesis",
      "One will fail; the fix is graceful degradation"
    ],
    "answer": 1,
    "why": "A human reads those as different questions; the workers run mostly the same searches and return heavily overlapping findings. The fix is a delegation-prompt fix — tell each worker what the others are covering, and share an already-searched list. Reaching for orchestration code here is fixing it in the wrong place."
  },
  {
    "q": "Which task shape is the <em>worst</em> fit for multi-agent?",
    "options": [
      "Surveying twenty companies' latest filings",
      "A sequential data migration where each step depends on the previous step's output",
      "Comparing four vendors across the same criteria",
      "Open-ended research across many sources"
    ],
    "answer": 1,
    "why": "Multi-agent needs parallelisable, read-heavy work. A sequential mutation chain is neither: workers cannot run concurrently, and the coordination overhead is pure loss on top of a task a single agent handles directly and more safely."
  },
  {
    "q": "Three workers independently return the same confident but wrong figure. What is the danger, and what defends against it?",
    "options": [
      "No danger — three independent confirmations is strong evidence",
      "The lead synthesises a fluent, false answer because agreement reads as corroboration; defend by requiring sources and checking source independence",
      "The lead will detect the error during synthesis automatically",
      "The turn cap will stop the run before synthesis"
    ],
    "answer": 1,
    "why": "Multi-agent structure amplifies this failure rather than catching it, because unanimity looks like corroboration. Require a source for every claim, have the lead verify the workers actually used <em>independent</em> sources, and treat unanimous high confidence on a hard question as a flag rather than reassurance."
  },
  {
    "q": "Your quality-versus-workers curve saturates around three to four workers while cost stays roughly linear. What follows?",
    "options": [
      "Use the maximum workers the budget allows",
      "The knee is where marginal quality per marginal dollar collapses — past it you are buying rounding error with real money",
      "The measurement is wrong; quality should scale with workers",
      "Switch to a single agent"
    ],
    "answer": 1,
    "why": "Saturating quality against near-linear cost means each additional worker buys less and costs the same. The knee is the defensible operating point, it is usually lower than people expect, and it is a Decision Log number — measured on <em>your</em> task, since the curve shape transfers but the knee does not."
  }
]
:::
