::: objectives
- Write a tool description that reads as a prompt, because it is one
- Choose between tool_choice modes and know what each costs in behaviour and tokens
- Consolidate a sprawling CRUD surface into tools shaped like tasks
- Design parameters that make the wrong call structurally difficult
- Write errors and returns that let a model recover without your help
- Diagnose a failing agent by fixing the tool layer and nothing else
:::

## The interface nobody designs

We spend careers on human-computer interfaces — affordances, error messages, undo, progressive
disclosure. Then we expose a language model to eight functions named `get_data`, `do_thing` and
`process`, with no parameter descriptions, and conclude from the resulting mess that the model is
bad at our domain.

An agent-tool interface is a user interface. Its user reads only text, has no memory of your
codebase, cannot ask a colleague, and must decide from the description alone whether this is the
right tool for what it is trying to do. It deserves the same care as a human interface and typically
gets a fraction of it.

::: key
Agent-tool interfaces are as critical as human-computer interfaces, and get a fraction of the care.
:::

## Anatomy of a tool definition

Three fields. The third is a schema; the second is a prompt.

```python title=tools/crm.py
CRM_SEARCH = {
    "name": "crm_search_customers",
    "description": (
        "Search the CRM for customers by name, email address or account number. "
        "Use this whenever you need a customer's internal ID before calling any "
        "other crm_* tool — no other tool accepts a name. "
        "Returns up to 20 matches, each with name, email, account_number, "
        "customer_id and status. "
        "Returns an empty list (not an error) when nothing matches: widen the "
        "query and retry rather than reporting failure to the user."
    ),
    "input_schema": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "Free text: a name, email address or account number.",
            },
            "status": {
                "type": "string",
                "enum": ["active", "churned", "trial", "any"],
                "description": "Filter by account status. Use 'any' unless the user narrowed it.",
            },
            "limit": {
                "type": "integer",
                "description": "Maximum results to return, 1-20. Defaults to 10.",
            },
        },
        "required": ["query"],
    },
}
```

Read that description again as though you were the model. It answers four questions, in order:

1. **What does it do?** Search customers by name, email or account number.
2. **When should I reach for it?** Whenever you need a customer ID — and it tells you the negative
   space too: no other tool takes a name, so this one is the entry point.
3. **What comes back?** Named fields, so the model can plan its next turn instead of guessing.
4. **What does failure look like?** Empty is not an error. Without that sentence, models routinely
   report "the customer does not exist" after a single narrow query.

Most tool descriptions in the wild answer only the first, in six words.

::: widget tool-linter
:::

## The hidden tax

Tool definitions are not free and they are not paid once. They occupy context on **every turn** of
every conversation, whether the tool is used or not, and enabling tools at all adds a system-prompt
surcharge on top.

Concretely: forty tools averaging 300 tokens each is roughly 12,000 tokens per turn, before the user
has typed anything. On a fifteen-turn task that is 180,000 tokens of pure definition — more than most
tasks need in total. This is a bill and a context-budget problem at the same time, and it is the
single most common way an MCP integration (M6) quietly ruins an agent.

Three consequences follow:

- **Every tool must earn its slot.** "It might be useful someday" is a cost you pay per turn.
- **Description length is a real trade-off.** Longer descriptions genuinely improve selection
  accuracy, up to a point. Pay for the sentences that change behaviour; cut the ones that restate.
- **Prompt caching changes the arithmetic, not the principle.** A cached tool block is much cheaper
  to re-send, but it still occupies attention. Caching fixes the invoice, not the context rot.

## Consolidation beats coverage

The instinct from API design is to mirror the backend: one tool per endpoint, CRUD for everything.
It produces a tool surface shaped like your database and nothing like the tasks users have.

Consider a calendar integration exposed as `list_events`, `get_event`, `create_event`,
`update_event`, `delete_event`, `list_attendees`, `add_attendee`, `check_availability`. To schedule a
meeting the model must chain five of them, each round trip adding latency, tokens and a chance to go
wrong.

Replace them with one tool shaped like the actual job:

```python
SCHEDULE_EVENT = {
    "name": "calendar_schedule_event",
    "description": (
        "Schedule a meeting: finds a slot when all attendees are free, creates the "
        "event and invites them, in one step. Use this for any request to set up, "
        "book or arrange a meeting. "
        "Attendees may be given as names or email addresses — names are resolved "
        "against the directory automatically. "
        "Returns the scheduled time, the event link, and the list of attendees who "
        "could not be invited. "
        "If no slot exists in the window, returns the three closest alternatives "
        "rather than an error."
    ),
    ...
}
```

One call instead of five. Fewer turns, less context, fewer failure points, and — critically — the
tricky logic (availability intersection, name resolution, fallback slots) lives in *your* code where
it is testable, instead of being improvised by the model across five round trips.

The rule of thumb: **name tools after tasks, not after tables.** If a tool's name is a verb from your
ORM, look again.

## Namespacing and the thirty-tool wall

Prefix by system: `crm_search_customers`, `calendar_schedule_event`, `github_open_pr`. This costs a
few tokens and buys three things — no collisions when you add a second integration, an obvious
grouping the model can reason about, and the prerequisite for tool search later.

Somewhere around thirty tools, "add another tool" stops working. Selection accuracy degrades before
context does: the model is choosing from a menu too long to hold clearly in mind, and near-duplicates
start attracting each other's calls. The fixes, in order of how much they change your architecture:

- **Consolidate.** Most large tool sets are a small number of tasks fragmented across many endpoints.
- **Split by agent.** Give each sub-agent (M9) only the tools its job needs.
- **Tool search.** Mark tools `defer_loading: true` and add a search tool
  (`tool_search_tool_regex_20251119` or the BM25 variant); the model retrieves the definitions it
  needs. Definitions are appended rather than swapped, so the prompt cache survives. Never defer
  every tool — the search tool itself and at least one other must stay loaded.

## Poka-yoke: making the wrong call hard

*Poka-yoke* is the manufacturing practice of designing parts that cannot be assembled wrongly. Apply
it to parameters.

**Closed sets become enums.** A `status` parameter typed as a free string invites `"cancelled"`,
`"CHURNED"`, `"inactive"` — three ways to be wrong. As an enum, the wrong value is a schema error you
never see rather than a silent empty result.

**Opaque identifiers say where they come from.** `"customer_id"` with no description gets invented.
`"The customer_id returned by crm_search_customers"` gets looked up.

**Booleans that switch behaviour become named modes.** `force=true` is a coin flip. `mode` with
`["dry_run", "apply"]` is self-documenting, and the safe value is the one whose name says what it
does.

**Structured objects over parallel arrays.** Two arrays that must line up by index will, eventually,
not line up.

**Use strict tool use for the ones that matter.** Setting `strict: true` on a tool definition (with
`additionalProperties: false` and a `required` list) guarantees the arguments validate against your
schema exactly — turning a class of malformed-argument bugs into something the API enforces rather
than something your handler discovers.

## Returns and errors are prompts too

Whatever a tool returns becomes context the model reasons from. Shape it deliberately.

**Resolve identifiers to names.** Returning `{"owner_id": "u_8823fa"}` forces another round trip or,
worse, an invented name. Return `{"owner": "Dana Ruiz", "owner_id": "u_8823fa"}` and the model can
write the sentence immediately.

**Return what is needed, not what is available.** A row with forty columns when the task needs three
is thirty-seven columns of attention spent for nothing, on every subsequent turn.

**Paginate with intent.** Say how many results exist, how many were returned, and exactly how to get
the next page. "Showing 10 of 47 matches. Call again with `page=2` for the next 10." — the model now
knows both that more exist and precisely what to do about it.

**Never truncate silently.** A tool that quietly cuts a 48,000-token document to 4,000 and returns it
unmarked produces confident, false answers with no error anywhere in the trace. If you truncate, say
so in the payload: `"[truncated: showing 4,000 of 48,000 tokens; call with offset=4000 to continue]"`.

**Make errors instructive.** Compare:

| Unhelpful | Instructive |
| --- | --- |
| `Error: invalid input` | `Error: 'status' must be one of active, churned, trial, any (got 'cancelled').` |
| `Error: not found` | `No customer matches 'j.smith'. Try a broader query, or search by account number.` |
| `Error: 500` | `The CRM is unavailable for scheduled maintenance until 14:00 UTC. No lookup will succeed this session — tell the user and stop retrying.` |

That last one is the one people miss. An error that tells the model retrying is futile is the
difference between a graceful stop and the error thrash you simulated in M1.

::: autopsy “The model is bad at this.”
Six real cases where a team concluded the model could not handle their domain. In every one, the
model was doing exactly what the tool layer told it to. A `get_records` whose description was
"gets records" — selected almost at random against its three neighbours. A date parameter with no
format specified — nine different formats emitted across a hundred runs. A search that returned
`[]` on no-match with no explanation — reported to users as "that customer doesn't exist" instead
of retried. A `limit` with no maximum documented — 5,000 rows requested, context blown. In the lab
you rewrite each description and watch the failure disappear without touching the agent.
:::

## Working with Claude on your tool layer

Here is a workflow that feels like cheating and is standard practice: give the failing transcripts to
Claude and ask it to diagnose the tool layer.

You have the eval failures, the tool definitions, and the traces. Paste them in and ask which tool
descriptions caused which failures, and what the rewrite should say. The model is unusually good at
this, for the obvious reason that it is the reader you are writing for. It will flag ambiguities you
cannot see because you know what you meant.

Do this after your own analysis, not instead of it, and treat the output as a proposal to test — the
eval set stays the judge.

::: lab Lab 2 — Tool surgery
`labs/lab02_tool_surgery` gives you eight deliberately-flawed CRM tools, a 20-task eval set, and a
harness that runs it. One rule: **only the tool layer may change.** The agent loop, the system prompt
and the eval set are frozen.

Baseline is roughly 55% pass rate. Get to 85% or better while *reducing* total tokens. The tests
check both, so you cannot buy accuracy with a thousand-word description.

Then run `python -m lab02_tool_surgery.review` to produce the diff summary you will hand to a
classmate for the 300-word design review the gate requires.
:::

## Further reading

- [Writing effective tools for agents](https://www.anthropic.com/engineering/writing-tools-for-agents)
- [Tool use overview](https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview) — schemas, `tool_choice`, strict tool use, parallel calls

::: quiz
[
  {
    "q": "Your agent has 40 tools averaging 300 tokens of definition. A task takes 15 turns. Roughly how many tokens do the definitions alone consume across the task?",
    "options": ["12,000", "180,000", "4,500", "300"],
    "answer": 1,
    "why": "12,000 tokens of definitions are re-sent on every one of the 15 turns — about 180,000 tokens before any conversation content. Caching reduces what you are billed for this, but the definitions still occupy the context window on every turn."
  },
  {
    "q": "A <code>status</code> parameter accepts \"active\", \"churned\" or \"trial\". It is currently typed as a plain string. What does adding an <code>enum</code> buy you?",
    "options": [
      "Nothing — the description already lists the values",
      "It converts a class of silent wrong answers into a schema error you never see",
      "It reduces the token cost of the definition",
      "It forces the model to always supply the parameter"
    ],
    "answer": 1,
    "why": "Without the enum, a plausible-but-wrong value like \"cancelled\" is accepted by the schema and produces an empty result the model interprets as \"no such customers\". The enum makes the wrong call structurally impossible rather than merely discouraged — that is poka-yoke."
  },
  {
    "q": "A tool reads a 48,000-token document and returns the first 4,000 tokens with no marker. What is the characteristic failure?",
    "options": [
      "The model errors and the run terminates",
      "The context window overflows on the next turn",
      "The model answers confidently about content it never saw, with nothing in the trace looking wrong",
      "The API rejects the oversized tool result"
    ],
    "answer": 2,
    "why": "Silent truncation is dangerous precisely because nothing errors. The model reasons over a partial document and states conclusions — \"there is no termination clause\" — with full confidence. Always mark truncation in the payload and say how to get the rest."
  },
  {
    "q": "You have a calendar integration exposing eight CRUD tools. Scheduling a meeting requires chaining five of them. What is the strongest argument for consolidating into one <code>schedule_event</code> tool?",
    "options": [
      "It uses fewer tokens of tool definition",
      "It moves the tricky logic into your code where it is testable, and removes four chances to go wrong",
      "CRUD tools are harder for the model to understand individually",
      "It lets you drop the namespacing prefix"
    ],
    "answer": 1,
    "why": "Token savings are real but secondary. The decisive gain is that availability intersection, name resolution and fallback slots become deterministic code you can unit-test, instead of being improvised by the model across five round trips — each of which adds latency and a failure point."
  },
  {
    "q": "Which error message is most likely to prevent an agent from burning its whole turn budget?",
    "options": [
      "\"Error: 500 internal server error\"",
      "\"Error: request failed, please try again\"",
      "\"The CRM is down for maintenance until 14:00 UTC. No lookup will succeed this session.\"",
      "\"Error: unable to complete operation\""
    ],
    "answer": 2,
    "why": "The model has no way to know a failure is permanent unless the error says so. The first, second and fourth all read as transient and invite retries — the error-thrash pattern from M1. Saying the failure is permanent lets the agent stop and report honestly."
  }
]
:::
