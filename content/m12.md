::: objectives
- Build a cost model before the rollout, and find its dominant term
- Use prompt caching in earnest, and detect the changes that silently break it
- Route work by difficulty without letting quality fall through the floor
- Understand why agent p95 latency is driven by turn count, not per-turn speed
- Make a stateful system survive faults with checkpointing and durable execution
- Deploy without disrupting agents that are mid-task
:::

## Have the conversation first

The prototype costs two dollars a day. Then it goes to 5,000 users and costs forty thousand a month,
and the conversation about whether that is acceptable happens *after* everyone is depending on it.

Have it first. The arithmetic takes twenty minutes.

::: key
The prototype cost $2 a day. The rollout costs $40,000 a month. Have this conversation first.
:::

## Build the model

Cost per task decomposes into five terms:

```text
cost_per_task =
      input_tokens  x turns x (cache-adjusted input price)
    + output_tokens x turns x output price
    + tool_definition_surcharge x turns
    + server_tool_fees                      (web search, code execution)
    x (1 + subagent_multiplier)             (M9)
```

The term that surprises people is the first, and specifically **how it grows**. On turn *n* you
re-send the entire transcript. Across a task of *n* turns, history tokens accumulate as
n(n−1)/2 — quadratic, not linear. A 20-turn task does not cost twice a 10-turn task. It costs
roughly four times.

This single fact reorders every optimisation you might attempt. Halving your turn count beats
halving your model price.

::: widget cost-model
:::

Prices change, so treat them as inputs rather than constants. Check the current published rates, put
them in one config, and make your cost model read from it. As a rough shape at the time of writing:
Haiku-class models sit around a dollar per million input tokens, Sonnet-class around three, Opus-class
around five, with output at roughly five times input — and cache reads roughly a tenth of the input
price. Verify before quoting.

## Prompt caching in earnest

Caching is the highest-leverage cost lever available and it is nearly free to adopt. The mechanics
from M3, applied with intent:

**Order for a stable prefix.** Requests render `tools` → `system` → `messages`. Everything stable
goes first, everything volatile last.

**Place breakpoints deliberately.** You get up to four. Typically: end of tools, end of system
prompt, and a rolling one in the conversation. Below roughly 1,024 tokens a prefix will not cache at
all — silently.

**Verify.** `usage.cache_read_input_tokens` is the only honest signal. Zero across repeated requests
means something is moving.

The changes that silently double your bill, all of which pass code review:

| Change | Why it breaks the cache |
| --- | --- |
| A timestamp in the system prompt | Prefix differs on every single request |
| Tool list built from a set or dict | Order varies between processes |
| Appending an instruction to `system` mid-session | The prefix changed; use a `{"role":"system"}` message instead |
| Switching model mid-session | Different model, different cache |
| Adding or removing a tool mid-session | Tools render first; use tool search, which appends |
| A per-request ID early in the prompt | Same as the timestamp |

Every row is invisible in testing and expensive in production. Put cache hit rate on a dashboard
(M10) so a regression is a graph, not a quarterly surprise.

## Model routing

Not every step needs your best model. Classification, extraction, simple formatting and much judging
run well on a small model; planning and synthesis need a strong one.

```python title=routing.py
def choose_model(step, complexity):
    """Route by step type, with a quality floor that overrides the cheap path."""
    if step in ("classify", "extract", "format"):
        return "claude-haiku-4-5"
    if step == "judge" and complexity < 0.6:
        return "claude-haiku-4-5"
    if step in ("plan", "synthesise"):
        return "claude-opus-5"
    return "claude-sonnet-5"
```

Two disciplines make routing safe rather than merely cheap:

**A quality floor.** Route cheap, then measure with your M8 suite *per route*. A route that drops
below its floor gets promoted back. Without this, routing is just a way to make your product worse
for money.

**Don't switch mid-session.** Changing model in the middle of a conversation invalidates the cache
and can cost more than the model saved. Spawn a sub-agent for the cheap sub-task and keep the main
loop on one model.

Also consider the Batch API for anything not latency-sensitive — overnight evals, bulk
classification, backfills. Roughly half price for work nobody is waiting on.

## Latency

The thing people get wrong: **agent p95 is dominated by turn count, not per-turn speed.**

A 400ms improvement per turn is 400ms on a one-turn task and meaningless against a task that
sometimes takes 25 turns. The tail is made of runs that took many more turns than the median, so the
levers that matter are:

- **Fewer turns.** Tool consolidation (M2) removes round trips wholesale.
- **Parallel tool execution.** Independent calls concurrently — the M1 lesson, and still the most
  common avoidable latency in production agents.
- **Streaming.** Does not reduce latency; substantially reduces *perceived* latency, which is what
  users report on.
- **Prefetching.** If a tool is almost always called first, call it before the model asks.
- **A turn cap tuned from data.** The p99 run that takes 40 turns is usually thrashing, not working.

## Reliability under statefulness

Stateless services taught us that a failed request is retried and forgotten. Agents are not stateless,
and the same fault has very different consequences.

A crash on turn 18 of a 20-turn task loses eighteen turns of work *and* the money spent producing
them. Do it under load and you have an outage that is also a bill.

**Checkpoint after every turn.** Messages, tool state, budget spent, memory pointers. Resumption
should be the normal recovery path, not an exotic one.

**Make tool calls idempotent, or make them safe to repeat.** If a resumed agent re-issues
`send_email`, an idempotency key is the difference between a resume and an incident.

**Durable execution.** For long-running agents, a workflow engine that persists state transitions
lets a worker die mid-task and another pick it up. The alternative — restart from zero — becomes
untenable exactly when tasks become valuable.

**Design tool errors an agent can recover from.** M2's lesson with money attached: an error that says
what is wrong and what to try instead converts a failed run into a degraded one.

## The retry storm

Naive retries turn a blip into an outage, and they do it in a way that looks locally reasonable.

Rate limit hits. Every in-flight agent retries immediately. The retries arrive together, are also
rate-limited, and retry again. Now you have more load than the original traffic, sustained, with the
provider's limiter guaranteeing every one of them fails — and each retry re-sends a full transcript,
so the bill grows while the success rate is zero.

Three things prevent it, and you need all three:

```python title=reliability.py
import random, time


def backoff_delay(attempt, base=1.0, cap=60.0):
    """Exponential with full jitter. The jitter is the part that matters:
    without it, every client retries in lockstep and re-creates the spike."""
    return random.uniform(0, min(cap, base * 2 ** attempt))


class CircuitBreaker:
    """Stop calling a service that is down. Retrying a hard-down dependency is
    load you generate against yourself."""

    def __init__(self, threshold=5, recovery_s=30):
        self.threshold, self.recovery_s = threshold, recovery_s
        self.failures, self.opened_at = 0, None

    def allow(self):
        if self.opened_at and time.time() - self.opened_at > self.recovery_s:
            self.opened_at, self.failures = None, 0     # half-open: try one
        return self.opened_at is None

    def record(self, ok):
        if ok:
            self.failures, self.opened_at = 0, None
        else:
            self.failures += 1
            if self.failures >= self.threshold:
                self.opened_at = time.time()
```

And the third: a **budget guard** that aborts the run when spend crosses the ceiling, regardless of
what the retry logic thinks it is doing. Retries are how a bug becomes expensive; the budget guard is
what bounds the damage.

## Graceful degradation and budget policy

Decide *in advance* what happens when a limit is hit. There are three answers and they are product
decisions, not engineering ones:

- **Degrade** — finish with a cheaper model, fewer workers, or a partial answer clearly marked as
  partial.
- **Escalate** — hand to a human with the work so far attached.
- **Abort** — stop and report honestly.

Whichever you choose, say it out loud in the Decision Log, and set budgets per session, per tenant
*and* per task. A per-session cap does not stop one tenant consuming everything.

## Rainbow deployments

The constraint stateless services never taught you: **you have agents mid-task during a deploy.**

Kill a pod and you kill a 20-turn task at turn 12. Multiply by concurrent load. A rolling deploy that
is invisible for a web service is a mass-cancellation event for an agent fleet.

Rainbow deployments shift traffic gradually and let old versions keep running until their in-flight
work completes. New tasks go to the new version; existing tasks finish where they started. It costs
you running two versions concurrently for a while, which means both must tolerate the same session
format — a compatibility constraint worth knowing about *before* you need to change that format.

::: autopsy The retry storm
An intermittent 429 from a downstream API. The client retried three times with no jitter. Two hundred
concurrent agents hit the limit within the same second, all retried at t+1s, all failed, all retried
at t+2s.

Sustained load rose to roughly six times baseline for eleven minutes. Every retry re-sent a full
transcript — averaging 40,000 tokens — so the bill for that window ran to four figures with a 0%
success rate. The dashboard showed nothing unusual until the invoice, because the agents were
"working": no errors surfaced to users, just slow tasks that eventually gave up.

Fixed with jittered exponential backoff, a circuit breaker on the downstream service, and a
per-session budget guard that aborts rather than retries into a wall. The alert that would have
caught it in minutes is cost per task, p95 — the first metric in M10's list.
:::

::: lab Lab 12a — Cut the bill by 60%
`labs/lab12_cost_chaos` — hit a 60% cost reduction on the reference agent while keeping M8 eval
accuracy within 3 points. Levers available: caching, model routing, context strategy, tool return
shaping, worker count. The tests check cost *and* accuracy, so buying one with the other fails.
:::

::: lab Lab 12b — Chaos
Run your M9 system through a fault-injecting proxy: timeouts, 500s, malformed payloads, rate limits,
slow responses. Reach 95% task completion. Then survive a simulated mid-task deployment with no lost
work.
:::

::: lab Lab 12c — Observatory v4
Cost attribution by tool, subagent and turn, plus a per-tenant budget dashboard. The gate requires it
to be accurate to within 5% of the billed usage — which is where you discover which costs you were
not counting.
:::

## Further reading

- [Prompt caching](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)
- [Message Batches API](https://docs.anthropic.com/en/docs/build-with-claude/batch-processing)
- [Pricing](https://www.anthropic.com/pricing)

::: quiz
[
  {
    "q": "A task takes 20 turns instead of 10, with everything else unchanged. Roughly what happens to input token cost?",
    "options": ["It doubles", "It roughly quadruples", "It stays the same due to caching", "It increases by the tool definition surcharge only"],
    "answer": 1,
    "why": "Each turn re-sends the whole transcript, so history tokens accumulate as n(n−1)/2 — quadratic in turn count. This is why halving turn count beats almost any other optimisation, including switching to a cheaper model."
  },
  {
    "q": "Which change to a working agent will silently double its bill?",
    "options": [
      "Adding a tool at startup",
      "Putting the current timestamp in the system prompt",
      "Increasing max_tokens",
      "Enabling streaming"
    ],
    "answer": 1,
    "why": "Caching is a prefix match. A timestamp in the system prompt changes the prefix on every request, so nothing ever caches — with no error, no warning and no behavioural change. Only <code>usage.cache_read_input_tokens</code> and your invoice can tell you."
  },
  {
    "q": "You want to reduce p95 latency for an agent whose median task is 8 turns and whose p95 is 25 turns. What helps most?",
    "options": [
      "A model with faster per-token output",
      "Reducing turn count — consolidating tools, executing independent calls in parallel, and capping thrashing runs",
      "Streaming the response",
      "Increasing max_tokens so fewer turns are truncated"
    ],
    "answer": 1,
    "why": "The tail is made of runs that took many more turns than the median, so per-turn speed barely moves it. Streaming improves perceived latency, which is worth doing, but it does not change the number. The p99 run at 40 turns is usually thrashing rather than working."
  },
  {
    "q": "Why is jitter the essential ingredient in retry backoff?",
    "options": [
      "It reduces the total number of retries",
      "Without it, all clients retry in lockstep and re-create the original load spike",
      "It makes retries idempotent",
      "It resets the circuit breaker"
    ],
    "answer": 1,
    "why": "Exponential backoff without jitter just moves every client's retry to the same later instant. Two hundred agents that failed together retry together, fail together, and sustain the outage. Full jitter spreads them out — and it pairs with a circuit breaker and a budget guard, which bound the damage the retries can do."
  },
  {
    "q": "Why do agents need rainbow deployments where a stateless web service does not?",
    "options": [
      "Agents use more memory per instance",
      "Killing an instance kills in-flight multi-turn tasks partway through, losing the work and the money already spent",
      "Agent binaries are larger and take longer to roll out",
      "Because model versions must match across instances"
    ],
    "answer": 1,
    "why": "A rolling deploy is invisible for stateless request handling and is a mass-cancellation event for a fleet of agents mid-task. Rainbow deployments route new tasks to the new version while letting existing tasks finish where they started — which requires both versions to tolerate the same session format."
  }
]
:::
