::: objectives
- Say why agent evaluation is structurally harder than model evaluation
- Distinguish outcome from trajectory evaluation, and use both
- Build a useful eval set from ~20 real cases instead of waiting for 500
- Write an LLM judge rubric, then validate the judge before trusting a number
- Run evals in CI under a cost cap, with stochasticity handled honestly
- Feed failures back into improvement without overfitting to your own set
:::

## The discipline that separates demos from products

Most agent projects that die do not die of a hard technical problem. They die because nobody could
tell whether a change made things better, so changes stopped being made, and the thing that worked
in the demo kept not working in production for reasons nobody could name.

::: key
If you cannot evaluate it, you cannot improve it, and you should not ship it.
:::

## Why agent eval is harder

Evaluating a single model call is comparatively easy: one input, one output, compare to a reference.
Agents break every one of those assumptions.

**Multi-step.** Failure can occur at any turn, and the *observable* failure is often several turns
downstream of the *actual* one.

**Stochastic.** The same input produces different trajectories. A single run tells you almost
nothing; you need repeats, and you need to look at variance.

**Path-dependent.** An early tool choice constrains everything after it. Two runs that both "fail"
may have failed for unrelated reasons.

**Many valid paths.** Often several trajectories reach an equally good answer. An eval that asserts
one specific path punishes correct behaviour and will be tuned away by whoever is trying to make the
number go up.

**Environmental.** Agents touch real systems. Your eval needs a fixture environment that is
deterministic enough to compare against and realistic enough to be worth comparing.

## Outcome and trajectory

**Outcome evaluation** asks: is the final answer right? Necessary, insufficient.

**Trajectory evaluation** asks: did it get there sensibly? Did it call the tools it should have,
avoid the ones it shouldn't, stay within a reasonable turn count, and not do anything alarming on the
way?

You need both, because outcome-only evaluation cannot distinguish an agent that reasoned correctly
from one that guessed. Consider a query with two plausible answers where the agent picks one at
random and happens to be right. Outcome: pass. Trajectory: it never called the tool that
disambiguates. That agent will fail tomorrow, and your eval suite just told you it was fine.

```python title=evals/case.py
from dataclasses import dataclass, field


@dataclass
class EvalCase:
    id: str
    prompt: str
    tier: str                                   # 'easy' | 'realistic' | 'hard'

    # Outcome assertions
    must_contain: list = field(default_factory=list)
    must_not_contain: list = field(default_factory=list)
    judge_rubric: str | None = None

    # Trajectory assertions
    must_call: list = field(default_factory=list)        # tools that must appear
    must_not_call: list = field(default_factory=list)    # tools that must not
    max_turns: int = 15
    max_usd: float = 0.25
```

Note `must_not_call` and `max_usd`. An agent that gets the right answer by calling
`delete_and_recreate`, or by spending four dollars, has not passed.

## Start with twenty cases

The most common reason a team has no eval set is that they are waiting until they can build a good
one. Do not wait.

Twenty real cases — drawn from actual usage, actual tickets, actual questions — is enough to catch
the failures that matter. Prompt changes have been observed moving success rates from around 30% to
around 80% at that sample size, because the first tranche of failures is usually systematic rather
than subtle. You do not need statistical power to find a systematic bug; you need examples.

How to pick them:

- **Sample from reality**, not from imagination. Your imagination produces the happy path.
- **Cover three tiers.** Easy (should always pass — these catch regressions), realistic (the actual
  distribution), hard (should sometimes fail — these are your headroom).
- **Include the ones you are afraid of.** The ambiguous request, the one where the right answer is
  "I don't know", the one where the correct action is to escalate.
- **Include cases that should fail closed.** The agent should refuse, abstain or ask. If nothing in
  your suite rewards abstention, you will train it out.

Then grow the set from production failures. Every incident becomes a case. This is the flywheel.

## LLM-as-judge

For anything with an open-ended output, you need a model to grade it. This works, and it fails in
specific documented ways.

A rubric with named dimensions beats "is this good?" by a wide margin:

```text
Score the response on each dimension. Cite evidence from the response for each score.

FACTUAL ACCURACY (0-3)  Are the claims correct per the source documents?
                        0 = contains a false claim; 3 = all claims verified.
CITATIONS (0-3)         Is every factual claim attributed to a specific source?
                        0 = no citations; 3 = every claim cited to document and location.
COMPLETENESS (0-3)      Does it address every part of the question?
                        An explicit "not available in the sources" counts as complete.
SOURCE QUALITY (0-3)    Are sources authoritative? 0 = SEO content farm or blog aggregator;
                        3 = primary sources or official documentation.
TOOL EFFICIENCY (0-3)   Was the work proportionate? Penalise redundant and repeated calls.

Output JSON: {"factual": n, "citations": n, "completeness": n,
              "source_quality": n, "tool_efficiency": n, "evidence": "..."}
PASS requires factual >= 2, citations >= 2, and no dimension at 0.
```

Note what that rubric had to say explicitly: that abstention counts as complete, and that SEO
aggregators are low-quality sources. Judges do not infer standards you did not write down.

### The biases, and what to do about them

**Position bias.** In pairwise comparison, judges favour one position. Mitigate by running both
orders and discarding disagreements, or by scoring absolutely rather than comparatively.

**Verbosity bias.** Longer, more fluent answers score higher regardless of substance. Mitigate with
explicit length-independent criteria, and by including a padded-but-wrong case in your judge
validation set.

**Self-preference.** Judges favour output from the same model family. Mitigate by using a different
model as judge, or by anchoring scores to reference answers.

**Leniency drift.** Judges pass borderline cases. Mitigate by anchoring each score with a concrete
example of what a 0 and a 3 look like.

## Validate the judge

An unvalidated judge is a random number generator with good manners. Before you trust it:

1. Hand-label 30 outputs yourself, against the rubric, **before** seeing the judge's labels.
2. Run the judge on the same 30.
3. Measure agreement — and measure Cohen's kappa, not raw agreement.
4. Read every disagreement. The judge is usually right about something you under-specified.
5. Fix the rubric. Re-measure. Repeat until kappa clears your threshold.

Raw agreement lies. On a set where 80% of cases pass, a judge that passes everything scores 80% and
is worthless. Kappa corrects for chance agreement and exposes it.

::: widget judge-agreement
:::

And the rule that has to be stated because the temptation is real: **when the judge and your labels
disagree, you fix the rubric, never the labels.** Adjusting the reference to match the judge is how
teams produce a suite that agrees with itself and nothing else.

## Human evaluation is irreplaceable

Sit down and read fifty transcripts. Not summaries — transcripts.

You will find things no automated eval was looking for, because automated evals only check what
someone thought to assert. The canonical example: an agent whose answers were factually correct and
well-cited, passing every check, which had quietly learned to prefer SEO content farms over
authoritative sources — because the farms were easier to extract clean quotes from. No rubric
dimension existed for source quality until a human noticed. Then it became one.

Budget for this. Every significant change, before it ships.

## Evals in CI

An eval suite that runs when someone remembers is not a control.

```yaml title=.github/workflows/evals.yml
- name: Agent evals
  run: python -m evals.run --tier easy,realistic --repeats 3 --cost-cap 5.00
  env:
    ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
```

Four things this needs to work in practice.

**Handle stochasticity honestly.** Run each case N times and set the threshold on the aggregate. A
single flaky failure must not block a merge, and a case that passes 2 of 5 times is *failing* even
though it is green sometimes. Report pass *rates*, not pass/fail.

**Cap the cost of the eval job itself.** An eval suite that can spend unboundedly on a PR is its own
incident. Cap it, and fail the job if the cap is hit.

**Tier what blocks the merge.** Easy tier must be 100% — any failure is a regression. Realistic tier
blocks below a threshold. Hard tier is reported, never blocking, or people will delete the hard
cases.

**Track cost and turn count as first-class metrics.** A change that keeps accuracy and doubles cost
is a regression. If your CI only reports accuracy, that regression ships.

## Metrics beyond accuracy

| Metric | Why it matters |
| --- | --- |
| Tokens per task | The cost model's dominant input (M12) |
| Tool calls per task | Efficiency, and an early oscillation signal |
| Wall-clock p50/p95 | p95 is what users experience |
| Cost per task | The number the business cares about |
| Failure taxonomy | *Which* failure class, not just how many |
| **Variance** | High variance at the same mean is a worse product |

Variance is the one that gets left out. An agent that is right 90% of the time with low variance is a
product. One that averages 90% by being brilliant on Mondays and useless on Tuesdays is a support
burden, and the mean hides it entirely.

## Self-improvement, and the trap

Feed failing transcripts back to a model with the tool definitions and prompts, and ask what went
wrong and what to change. It is remarkably effective — reported efficiency gains in the region of
40% — for the same reason as in M2: it is the reader you are writing for.

The trap is overfitting. Iterating against the same twenty cases eventually produces a system tuned
to those twenty cases. Guard it: **hold out cases the improvement loop never sees**, refresh the set
from production regularly, and treat a suspiciously large jump as evidence of leakage rather than
success.

::: autopsy The 95% agent that failed in production
Passing 95% in the suite, visibly bad in the wild. The suite had been written by the team, from
memory, in an afternoon — which meant it encoded the questions they *expected*. Every case was
well-formed, unambiguous, answerable from one document, and phrased the way an engineer phrases
things.

Real traffic was none of those. Users asked compound questions, referenced things not in the corpus,
phrased requests ambiguously, and frequently wanted an answer the sources could not support. The
agent had never been evaluated on abstention, so it did what it had been optimised to do: answer
confidently.

The fix was procedural, not technical. Sample eval cases from *actual logs*, stratified — including
the messy, the ambiguous and the unanswerable — and add every production failure to the set as a
permanent case.
:::

::: lab Lab 8a — Evaluate M5's agent
`labs/lab08_evals` — build a suite of at least 25 cases across three tiers, with both outcome and
trajectory assertions and a written judge rubric.

Then run your suite against **another student's** agent. Cross-evaluation exposes rubric ambiguity
instantly: every place your rubric assumed something about your own implementation shows up as a
bogus failure.
:::

::: lab Lab 8b — Judge validation
Hand-label 30 outputs before looking at the judge. Measure raw agreement and kappa, read every
disagreement, fix the rubric, re-measure. `python -m lab08_evals.judge_validate` does the arithmetic
and reports the confusion matrix; you do the reading.
:::

::: lab Lab 8c — Observatory v2
Side-by-side eval run comparison: per-case deltas, regression highlighting, and cost and turn-count
diffs alongside accuracy. The most useful screen you will build all course.
:::

## Further reading

- [Evaluating agents](https://docs.anthropic.com/en/docs/test-and-evaluate/eval-tool)
- [Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)

::: quiz
[
  {
    "q": "An agent answers a question correctly without ever calling the tool that disambiguates it — it guessed between two plausible answers and got lucky. Which evaluation catches this?",
    "options": ["Outcome evaluation", "Trajectory evaluation", "LLM-as-judge on the final answer", "A larger eval set"],
    "answer": 1,
    "why": "Outcome evaluation — including a judge reading the final answer — sees a correct response and passes it. Only trajectory evaluation, asserting which tools were and were not called, distinguishes reasoning from a lucky guess. That agent will fail tomorrow and your suite just certified it."
  },
  {
    "q": "Your judge agrees with your human labels 84% of the time on a set where 80% of cases pass. What should you conclude?",
    "options": [
      "The judge is good enough to ship",
      "Almost nothing — a judge that passes everything would score 80%, so you need kappa to see whether 84% beats chance",
      "The judge is biased against passing cases",
      "The human labels are probably wrong"
    ],
    "answer": 1,
    "why": "Raw agreement is dominated by the base rate. Cohen's kappa corrects for the agreement you would get by chance, and on this set an 84% raw score may be close to worthless. Compute kappa, then read every disagreement — the judge is usually right about something the rubric under-specified."
  },
  {
    "q": "You are adding evals to CI. Which policy is soundest?",
    "options": [
      "Any single case failure blocks the merge",
      "Run each case once; retry the whole suite if anything fails",
      "Run each case N times, block on aggregate pass rates by tier, and cap the eval job's spend",
      "Run only the hard tier, since easy cases always pass"
    ],
    "answer": 2,
    "why": "Agents are stochastic, so single runs are noise and pass <em>rates</em> are the honest measure. Tiering keeps easy-tier regressions blocking while stopping people from deleting hard cases to get green. And an eval job that can spend unboundedly on a PR is its own incident."
  },
  {
    "q": "Manual transcript review famously caught an agent preferring SEO content farms to authoritative sources. Why did no automated eval catch it first?",
    "options": [
      "The judge model was too small",
      "Automated evals only check what someone thought to assert, and no rubric dimension for source quality existed yet",
      "The eval set was too small to reach significance",
      "The failures were intermittent"
    ],
    "answer": 1,
    "why": "The answers were factually correct and properly cited, so every existing check passed. Automated evaluation is exhaustive only over the dimensions you wrote down; discovering a dimension you did not know you needed is what human review is for. Afterwards it becomes a rubric dimension — and stays caught."
  },
  {
    "q": "Two agents both average 90% success. One is consistent; the other alternates between excellent and useless. Why does this matter, and where does it show up?",
    "options": [
      "It does not matter — the mean is the mean",
      "Variance determines user trust and support load, and reporting only the mean hides it entirely",
      "The inconsistent one is cheaper to run",
      "It only matters if the variance correlates with input difficulty"
    ],
    "answer": 1,
    "why": "Users experience the distribution, not the mean. High variance at the same average produces a product people cannot rely on and a support burden — and it is invisible in any report that shows only average accuracy, which is why variance belongs in the metrics table alongside cost and p95 latency."
  }
]
:::
